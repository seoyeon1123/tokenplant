# TokenPlant — 설계 근거

숫자와 규칙을 **왜 그렇게 정했는지**. 화면에는 안 보이지만 바꿀 때 알아야 하는 것들.
쓰는 법은 [README](README.md), 포크 경위는 [FORK.md](FORK.md), 명세는 [SPEC.md](SPEC.md).

### 두 물길이 서로 안 뺏는다

PokeTokenBar 는 재화가 곧 사용한 토큰이다 — `쓸 수 있는 재화 = 쓴 토큰 − 쓴 재화`.
성장 게이지와 상점 지갑이 **같은 값**이라, 이상한 사탕을 XP 값어치의 **5배**로 매겨야만
구매가 순손실이 되어 무한 자가성장이 막혔다. 자기네 소스 주석에 그 고민이 남아 있다.

여기서는 그 문제가 없다. 성장은 **가중 환산 mL**, 지갑은 **원시 토큰** — 같은 강에서
갈린 두 물길이라 서로 뺏지 않는다. 상점에서 다 써도 이미 자란 건 1mL도 안 줄어든다.

그래서 가속의 천장을 **가격이 아니라 수입**이 막는다:

> 지갑 하루치를 통째로 물에 쏟으면 하루 자동 성장만큼 **더** 자란다. 즉 최대 2배.

이 한 줄이 밸런스 전체를 고정한다. 아무리 부지런해도 사이클이 절반 밑으로 안 내려가고,
아무것도 안 사도 게임이 멈추지 않는다. 루프가 닫히지 않는 이유이기도 하다 —
살 수 있는 양이 결국 실제로 쓴 토큰에 묶여 있다.

### 하루에 줄 수 있는 양에 상한이 있다

지갑은 **계속 이월된다.** 그래서 상한이 없으면 며칠 안 쓰고 모았다가 하루에 다 쏟을 수
있었고, 실제로 30일치를 모으면 **하루 만에** 한 사이클이 끝났다(물 98개). 4주짜리
사이클이 하루로 무너지면 나머지 설계가 전부 의미를 잃는다.

"최대 2배"는 하루 **수입** 기준 평균이었지, 하루에 **쓸 수 있는 양**의 상한이 아니었다.
그 구분을 안 해둔 게 구멍이었다.

```
물     하루 5번        (5개 = 하루 자동 성장량 → 최대 2배)
영양제  한 그루에 1번
거름    돌고 있는 동안 못 씀
```

상한은 **횟수**로 센다. mL 로 세면 화면에 쓸 수가 없다 —
"오늘 12,074mL 남음"보다 **"오늘 3/5"** 가 읽힌다.

영양제만 따로 두는 이유: **비례 품목이라 횟수로는 안 막힌다.** 갓 심었을 때 한 개가
자동 성장 5.6일치다(값은 2.5일치). 물과 똑같이 하루 5회로 세면 영양제만 다섯 번 써서
나흘 만에 끝난다.

| 규칙 | 이식까지 | 가속 |
|---|---:|---:|
| 아무것도 안 사면 | 28일 | 1.0배 |
| 물 5개/일 | 15일 | 1.9배 |
| 물 + 영양제 + 거름 전부 (제일 부지런) | 11일 | 2.5배 |
| 영양제만 5개/일 (횟수만 셀 때) | 4일 | 7.0배 |
| 상한 없음 (고치기 전) | 2일 | 14.0배 |

두 가지는 지킨다:

- **지갑은 안 탄다.** 못 준 물이 사라지는 게 아니라 다음 날로 밀릴 뿐이다.
  며칠 자리를 비워도 손해가 없어야 한다.
- **막힌 아이템은 소모되지 않는다.** 먼저 consume 하고 나서 막으면 산 물이 그냥 사라진다.

### 세 아이템이 **모양**이 다르다

값만 다르고 하는 일이 같으면 제일 싼 것만 사게 된다. 그래서 다른 축에 뒀다.

| | 모양 | 효과 | 언제 값을 하나 |
|---|---|---|---|
| **물** | 정액 · 즉시 | +3,018 mL | 언제나 같다. 지금 한 칸 더 |
| **거름** | 배율 · 7일 지속 | 들어오는 물 +25% | 앞으로 꾸준히 쓸 때만 |
| **영양제** | 비례 · 즉시 | 남은 거리의 20% | 사이클 절반까지 이득, 그 뒤론 손해 |

거름은 물보다 17% 낫고, 영양제는 **사이클 절반(14일째)에서 갈린다** —
갓 심었으면 물의 2.1배, 거목 앞에서는 손해다.
상점은 그걸 숨기지 않는다: 지금 사면 물보다 이득인지 손해인지를 실수치로 적는다.

> 처음엔 영양제가 10%였는데, 그러면 이득 구간이 **사이클 첫 1.5일뿐**이라
> 사실상 죽은 품목이었다. 화분 슬롯도 10일이었는데 영구히 2배라 다른 구매를 다 압도했다.
> 반짝 부적은 반대로 20일 내고 30그루 동안 0.47마리라 너무 희박했다.

### 종은 색이 아니라 **꽃 모양**으로 갈린다

한동안 `PotSprites.grid(stageIndex:)` 가 종을 인자로 받지 않았다. 15종이 전부
같은 실루엣에 잎 색만 달랐다 — 색만 다른 걸 다양하다고 할 수는 없다.

종마다 실루엣 10장을 그리면 150장이고, 그만큼 그리면 대부분 못 그린 그림이 된다.
그래서 **잎은 공용으로 두고 꽃만 갈아끼운다.** 사람이 알아보는 건 결국 꽃이고,
잎 덩이는 종이 달라도 비슷하게 생겼다.

```
화분 그림 = 공용 잎 실루엣 10단계  +  꽃 모티프 스탬프  +  종 팔레트
                                      (big 7×5 · small 5×3 · fruit 3×2)
```

꽃 자리는 새로 정하지 않았다. 원래 실루엣에 이미 박혀 있던 꽃 덩이의 **무게중심**을
뽑아 앵커로 굳혔다(`BloomLayout.anchors`). 단계가 오르면 붙는 꽃이 늘어난다 —
봉오리 1 → 꽃 1 → 꽃 3 → 꽃 3 + 열매 3 → 거목 왕관 3.

손으로 그리는 건 모티프 15종 × 3장 = 45장이고, 전부 3×2 ~ 7×5 짜리다.
아웃라인·음영은 여전히 `decorate()` 가 만든다.

**이식해도 꽃이 안 진다.** 정원 스프라이트(16×16)에는 원래 꽃이 **하나도 없었다** —
다 키워서 옮겼더니 초록 덩이만 늘어서, 서른 그루를 모아도 보상이 나무밭이었다.
모으는 게 목적인 화면에서 그건 치명적이라, 같은 모티프를 작게(5×3) 얹었다.
꽃 자리는 잎 덩이 **안쪽**이어야 한다(가장자리에 걸치면 허공에 뜬 것처럼 보인다) —
모양 3종 × 모티프 15종을 전부 곱해서 `check_data.py` 가 확인한다.

**초반 다섯 단계는 일부러 공용이다.** 씨앗은 실제로도 다 비슷하게 생겼고,
여기서부터 갈라면 결국 종당 10장을 그려야 한다. 타협이라 테스트로 못 박아 뒀다
(`testEarlyStagesAreDeliberatelyShared`).

꽃이 없는 종(상추·몬스테라·대나무)은 잎 특징을 모티프 자리에 넣었다 —
주름진 잎, 갈라진 잎맥, 좁고 뾰족한 잎.

### 값의 단위는 토큰 수가 아니라 **일**이다

`157,500,000` 은 그냥 큰 숫자다. 하루만 모으면 되는 건지 한 달을 써야 하는 건지 알 수 없고,
그러면 상점은 "영영 못 사는 곳"으로 읽힌다. 그래서 값을 일로 정하고 토큰 수를 파생값으로 뒀다.

| 품목 | 일 | 원시 토큰 |
|---|---|---|
| 물 | 0.2 | 21M |
| 벤치 | 1 | 105M |
| 거름 | 1.5 | 158M |
| 새 모이통 | 2 | 210M |
| 영양제 | 2.5 | 263M |
| 석등 | 3 | 315M |
| 고급 씨앗 | 4 | 420M |
| 전설 씨앗 | 13 | 1.37B |
| 화분 슬롯 | 20 | 2.10B |
| 반짝 부적 | 20 | 2.10B |

**하루 유입은 앱이 직접 잰다** (최근 14일 달력일 평균, 표본 3일 미만이면 기본값 105M).
사람마다 10배씩 다르므로, 남의 평균으로 "1.5일치"라고 쓰면 그건 거짓말이다.
일수 자체는 고정이다 — 토큰을 많이 쓴 날 가격표가 흔들리면 아무것도 계획할 수 없다.

### 원시 토큰을 그대로 안 쓰는 이유

실측에서 **캐시 읽기가 원시의 98%** 였다. 그대로 세면 성장이 곧 캐시 읽기 양이 되어,
읽기만 한 날과 코드를 쏟아낸 날이 똑같이 자란다.

```
물(mL) = (출력×5 + 입력×1 + 캐시생성×1.25 + 캐시읽기×0.1) ÷ 1,000
```

### 하루 상한이 없는 이유

실측 하루 유입은 **최대가 중위의 11배**로 요동친다(중위 7,552 / 평균 17,655 / 최대 83,717 mL).
한때 상한과 이월 은행을 뒀는데, 무거운 날 이틀치가 자라 버리는 걸 막으려던 거였다.

지금은 없앴다. 요동치는 게 곧 실제로 일한 양이고, 그걸 잘라내면 그날 일한 값이 사라진다.
평탄화는 임계값 쪽에서 한다 — 뒤로 갈수록 구간이 배씩 벌어져서, 무거운 하루가
초반엔 세 단계를 올리지만 후반엔 한 칸도 못 채운다.

첫 설치에만 상한이 있다(성장 30,000mL · 지갑 200M).
전체 로그가 소급되면 첫 화면부터 거목에 지갑이 가득이다.

### 방치해도 진행도는 안 깎인다

3일부터 색이 마르고 7일에 완전히 마른다. 하지만 **누적 물은 1mL도 안 줄어든다.**
토큰을 안 쓰면 안 자라는 것 자체가 이미 결과인데, 거기에 진행도까지 뺏으면
실제로 쓴 토큰을 두 번 벌주는 셈이다. 한 번만 자라면 색은 바로 돌아온다.

### 만료되는 한도를 지갑으로

5시간 창과 주간 창은 **안 쓰면 사라진다.** 누적 토큰만 보는 설계는 그 만료를 모른다.
그래서 창을 다 태운 것 자체에 값을 쳐준다 — 세션 창 **+10M**, 주간 창 **+50M**.
엣지 트리거다(100% 를 새로 넘는 순간 1회, 리셋되면 재무장).

성장이 아니라 **지갑**으로 넣는다. 만료되는 자원을 남는 것으로 바꾸는 게 목적이라,
성장에 바로 꽂으면 "안 쓰면 사라지는 걸 건졌다"는 실감이 안 난다.
한 사이클에 약 260M — 사이클 수입의 9% 정도다.

한도는 Claude Code 자격증명(Keychain)의 OAuth 토큰으로 `api/oauth/usage` 를,
Codex 는 CLI 의 `account/rateLimits/read` 를 읽는다. 못 읽어도 적립은 그대로 굴러간다.
