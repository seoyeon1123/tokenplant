# TokenPlant 설계서

> AI 코딩 토큰을 쓰면 픽셀 식물이 자라는 macOS 메뉴바 앱.
> **쓰면 저절로 자라고**, 같은 토큰이 지갑에도 쌓여 물·거름·영양제를 사면 **더 빨리** 자란다.
> 거목이 되면 정원으로 이식되고 화분에는 새 씨앗이 심긴다.

Swift 5,126줄 + 테스트 2,726줄 · 2026-09-14

---

## 1. 구조 — 두 물길

쓴 토큰 하나가 두 곳으로 간다. 이 갈래가 이 앱의 전부다.

```
                ┌─▶ 가중 환산 mL ──▶ 화분이 저절로 자란다
쓴 토큰 ────────┤                    (버튼도, 앱을 여는 것도 필요 없다)
                └─▶ 원시 토큰 ────▶ 지갑 · 물/거름/영양제를 산다 ─▶ 가속
                                     (메뉴바에 뜨는 그 숫자)
```

둘은 **서로 뺏지 않는다.** 상점에서 다 써도 이미 자란 건 1mL도 안 줄어든다.

원본 PokeTokenBar는 `쓸 수 있는 재화 = 쓴 토큰 − 쓴 재화`라 성장 게이지와 지갑이 같은 값이었고,
그래서 아이템을 값어치의 5배로 매겨야만 무한 자가성장이 막혔다.
여기는 같은 강에서 물길이 둘로 갈릴 뿐이라 그 문제가 없다.

> **가속의 천장은 가격이 아니라 수입이 막는다.**
> 지갑 하루치를 통째로 물에 쏟으면 하루 자동 성장만큼 더 자라서 **최대 2배**다.
> 아무리 부지런해도 사이클이 절반 밑으로 안 내려가고, 아무것도 안 사도 멈추지 않는다.

**뒤집은 것** — 한때 이 둘을 하나로 합쳐 "붓는 것 자체가 지출"인 단일 화폐로 만든 적이 있다.
이중 사용을 피하려던 건데 애초에 여기 해당이 없는 문제였다.
그 바람에 *쓰면 자란다*는 약속이 사라져 앱을 안 열면 화분이 멈췄다. 되돌렸다.

---

## 2. 환산 — 토큰을 물로 바꾸는 법

원시 토큰을 그대로 세지 않는다. 실측에서 **캐시 읽기가 원시의 98%**였다.
그대로 쓰면 성장이 곧 캐시 읽기 양이 되어, 읽기만 한 날과 코드를 쏟아낸 날이 똑같이 자란다.
API 가격 비중을 따라 가중치를 매기면 출력이 최소한의 발언권을 갖는다.

```
물(mL) = (출력×5 + 입력×1 + 캐시생성×1.25 + 캐시읽기×0.1) ÷ 1,000
```

| | 값 |
|---|---|
| 1 mL를 만드는 원시 토큰 | 6,957 |
| 하루 원시 토큰 (실측) | 105,000,000 |
| 하루 자동 성장 | 15,092 mL |
| 이식 한 사이클 | 26.5일 (가만히) / 13일 (부지런히) |

### 하루 상한이 없는 이유

실측 하루 유입은 **최대가 중위의 11배**로 요동친다 (중위 7,552 / 평균 17,655 / 최대 83,717 mL).
한때 상한 20,000mL와 이월 은행을 뒀는데 없앴다 — 요동치는 게 곧 실제로 일한 양이고,
그걸 잘라내면 그날 일한 값이 사라진다. 평탄화는 **임계값 쪽**에서 한다.

첫 설치에만 상한이 있다 (성장 30,000mL · 지갑 200M).
전체 로그가 소급되면 첫 화면부터 거목에 지갑이 가득이다.

---

## 3. 성장 — 10단계, 그리고 이식

앞쪽을 촘촘히 둔 건 의도다. 첫날 안에 세 단계가 지나가야 다음날 또 열어본다.
뒤로 갈수록 구간이 배씩 벌어져서 **마지막 한 칸이 전체의 1/3**이다.
무거운 하루가 초반엔 세 단계를 올리지만 후반엔 한 칸도 못 채운다. 이게 상한 대신 쓰는 평탄화다.

| Lv | 단계 | 누적 mL | 구간 폭 | 가만히 두면 |
|---:|---|---:|---:|---:|
| 1 | 씨앗 | 0 | — | — |
| 2 | 발아 | 1,500 | 1,500 | 2시간 |
| 3 | 떡잎 | 5,000 | 3,500 | 8시간 |
| 4 | 어린잎 | 12,000 | 7,000 | 19시간 |
| 5 | 자란 줄기 | 27,000 | 15,000 | 1.8일 |
| 6 | 꽃봉오리 | 52,000 | 25,000 | 3.4일 |
| 7 | 첫 꽃 | 94,000 | 42,000 | 6.2일 |
| 8 | 만개 | 157,000 | 63,000 | 10.4일 |
| 9 | 열매 | 247,000 | 90,000 | 16.4일 |
| 10 | 거목 | 375,000 | 128,000 | 24.9일 |
| — | **이식** | 400,000 | 25,000 | **26.5일** |

### 보너스와 목마름

**스트릭**은 연속으로 *토큰을 쓴 날*을 센다 (`+min(N×2, 50)%`).
앱을 연 날이 아니다 — 열어보지 않아도 자라는 게 약속이라, 출석을 세면 그 약속이 깨진다.

**목마름**은 3일부터 색이 마르고 7일에 완전히 마른다. 하지만 **누적 물은 1mL도 안 줄어든다.**
토큰을 안 쓰면 안 자라는 것 자체가 이미 결과인데, 거기에 진행도까지 뺏으면
실제로 쓴 토큰을 두 번 벌주는 셈이다. 한 번만 자라면 색은 바로 돌아온다.

**뒤집은 것** — 원래는 방치하면 누적 물이 깎이는 감쇠(낙엽)가 있었다. 통째로 뺐다.

---

## 4. 상점 — 값의 단위는 토큰 수가 아니라 "일"이다

`157,500,000`은 그냥 큰 숫자다. 하루만 모으면 되는 건지 한 달을 써야 하는 건지 알 수 없고,
그러면 상점은 "영영 못 사는 곳"으로 읽힌다. 그래서 값을 일로 정하고 토큰 수를 파생값으로 뒀다.

```
가격 = 일수 × 하루 유입     // 하루 유입은 앱이 최근 14일 달력일 평균으로 직접 잰다
```

사람마다 하루 유입이 10배씩 다르므로, 남의 평균으로 "1.5일치"라고 쓰면 그건 거짓말이다.
일수 자체는 고정이다 — 토큰을 많이 쓴 날 가격표가 흔들리면 아무것도 계획할 수 없다.
표본이 3일 미만이면 측정을 안 믿고 기본값(105M)으로 물러난다.

### 10품목 (하루 105M 기준)

| 품목 | 일 | 원시 토큰 | 효과 |
|---|---:|---:|---|
| 물 | 0.2 | 21M | **정액** · 바로 최근 하루 성장의 1/5 |
| 벤치 | 1 | 105M | 장식 · 효과 없음 |
| 거름 | 1.5 | 158M | **배율** · 7일간 들어오는 물 +25% |
| 새 모이통 | 2 | 210M | 장식 · 효과 없음 |
| 영양제 | 2.5 | 263M | **비례** · 남은 거리의 20% |
| 석등 | 3 | 315M | 장식 · 효과 없음 |
| 고급 씨앗 | 4 | 420M | 다음 씨앗 희귀 이상 확정 |
| 전설 씨앗 | 13 | 1.37B | 다음 씨앗 전설 확정 |
| 화분 슬롯 | 20 | 2.10B | 동시에 두 그루 (영구) |
| 반짝 부적 | 20 | 2.10B | 반짝 확률 1/128 → 1/32 (영구) |

### 세 소모품이 *모양*이 다른 이유

값만 다르고 하는 일이 같으면 제일 싼 것만 사게 된다. 그래서 다른 축에 뒀다 —
갈리는 건 값이 아니라 **언제 쓰느냐**다.

| 품목 | 모양 | 언제 값을 하나 | 물 대비 |
|---|---|---|---|
| 물 | 정액 · 즉시 | 언제나 같다. 비교의 자 | 1.00× |
| 거름 | 배율 · 7일 | 앞으로 꾸준히 쓸 때만 | 1.17× |
| 영양제 | 비례 · 즉시 | 사이클 절반(14일째)에서 갈림 | 2.1× → 손해 |

상점은 이걸 숨기지 않는다. 거름·영양제 줄에 **같은 값으로 물을 샀을 때와 견준 손익**을
실수치로 적는다 — `(물보다 +3,776)` / `(물보다 −22,425 손해)`.
모든 값에 "며칠치"가 붙고, 부족분은 **"내일이면 / 3일 더"**로 번역된다.

**고친 것** — 영양제는 원래 10%였는데 이득 구간이 사이클 첫 1.5일뿐이라 사실상 죽은 품목이었다.
화분 슬롯은 10일이었는데 영구히 2배라 다른 구매를 전부 압도했다.
반짝 부적은 반대로 20일 값에 30그루 동안 0.47마리라 너무 희박했다. 셋 다 고쳤다.

### 만료되는 한도를 지갑으로

5시간 창과 주간 창은 **안 쓰면 사라진다.** 누적 토큰만 보는 설계는 그 만료를 모른다.
그래서 창을 100%까지 태운 것 자체에 값을 쳐준다 — 세션 창 **+10M**, 주간 창 **+50M**.
엣지 트리거다 (100%를 새로 넘는 순간 1회, 리셋되면 재무장).

성장이 아니라 지갑으로 넣는 이유: 만료되는 자원을 **남는 것**으로 바꾸는 게 목적이라,
성장에 바로 꽂으면 "사라지는 걸 건졌다"는 실감이 안 난다.

---

## 5. 정원 — 화분 위의 상위 루프

화분은 하나(또는 슬롯을 사면 둘)뿐이지만 정원은 계속 넓어진다.
거목이 되면 그 그루는 정원으로 옮겨지고 화분에는 새 씨앗이 심긴다.
**이식은 수동**이다 — 자동으로 넘기면 거목을 못 보고 지나친다.

| 필요 그루 | 티어 | 해금 |
|---:|---|---|
| 0 | 창가 | 화분만. 시작 지점 |
| 1 | 베란다 | 첫 그루가 화분 옆에 |
| 3 | 작은 화단 | 정원이 별도 창으로 열린다 |
| 6 | 뒷마당 | 그루마다 명패 |
| 10 | 온실 | 계절·밤낮이 시스템 시간을 따라간다 |
| 18 | 수목원 | 연못. 정원 PNG 내보내기 |
| 30 | 비밀의 숲 | 정원 만렙 |

### 종 15개 = 실루엣 3 × 팔레트

종마다 그림을 새로 그리지 않는다. 실루엣은 **둥근 / 길쭉한 / 낮고 퍼진** 세 종류뿐이고,
종 구분은 잎 색 팔레트로 한다. 반짝 변종도 금색 팔레트 하나로 끝난다.
18그루쯤 되면 실루엣 3종이 반복을 가려주는 게 눈에 보인다 — 1종일 때는 벽처럼 보였다.

| 등급 | 종 | 가중치 | 뽑힐 확률 | 예 |
|---|---:|---:|---:|---|
| 흔함 | 5 | 620 | 68.0% | 민들레 · 방울토마토 · 해바라기 |
| 보통 | 4 | 250 | 21.9% | 장미 · 튤립 · 라벤더 · 몬스테라 |
| 희귀 | 4 | 100 | 8.8% | 대나무 · 단풍 · 벚나무 · 꽃선인장 |
| 전설 | 2 | 30 | 1.3% | 세계수 · 무지개꽃 |

배경은 스프라이트를 그리지 않고 **절차적으로 조합**한다
(하늘 · 잔디 디더 · 울타리 · 유리 격자 · 길 · 연못).
깊이는 크기가 아니라 **색**으로 만든다 — 뒷줄은 하늘색 쪽으로 22% 물린다.
픽셀을 축소하면 깨지기 때문이다. 밤에는 잎도 같이 어둡게 한다
(배경만 어둡게 하면 나무가 형광처럼 뜬다).

> 한 그루 26.5일 × 30그루 = **2.2년**. 화분 슬롯을 사면 두 그루가 같이 자라서 **1.1년**.
> 정원 만렙을 노린다면 슬롯이 사실상 첫 목표다.

그루를 클릭하면 명패가 열린다 — 종 · 심은 날 · 완성한 날 · 걸린 일수 · 부은 총 물 ·
반짝 여부 · 붙인 이름. 이게 정원을 보는 이유다. 그루마다 그때 무슨 작업을 했는지가 붙어 있다.

---

## 6. 만든 것과 검증

`Core/`는 Foundation 전용이라 (AppKit·SwiftUI 없음) UI를 건드리기 전에 밸런스만 먼저 테스트할 수 있다.
원본 PokeTokenBar(MIT)에 합칠 때는 `applyUsage` 자리에 `PlantStore.update`를 꽂으면 되고,
네트워크 의존(PokeAPI)은 통째로 사라진다 — 스프라이트가 전부 로컬 픽셀맵이라서.

| | 값 |
|---|---|
| Swift 테스트 | 194개 |
| 파이썬 대조 단정 | 235개 |
| Swift 파일 | 26개 |
| 컴파일러 없는 검사기 | 4개 |

Swift 툴체인을 클라우드·로컬 VM 어느 쪽에도 못 깔아서(프록시 allowlist) 검사기를 따로 만들었다.
타입 검사는 못 하지만 **리팩터로 반쪽만 지운 심볼**과 **밸런스 회귀**는 여기서 잡힌다.
네 개 모두 일부러 코드를 망가뜨려 잡히는 걸 확인했다.

```bash
python3 verify/check_syntax.py .    # tree-sitter-swift 파싱
python3 verify/check_members.py .   # 없는 멤버 · 없는 case · 빠진 case · 사라진 타입
python3 verify/check_data.py        # 스프라이트 격자 · 가격표 양언어 일치 · 테스트 리터럴
python3 verify/run_tests.py         # 엔진을 파이썬으로 포팅해 같은 계산을 돌린다
```

**가격표 양언어 일치**가 특히 값을 한다. 파이썬 포팅은 Swift 없이 돌리는 유일한 오라클인데,
두 가격표가 갈리면 단정 235개가 전부 *존재하지 않는 앱*을 검증하게 된다.

---

## 7. 아직 안 한 것

- **Mac에서 전체 테스트 통과 확인.** 빌드는 되고 대부분 통과하지만 마지막 크래시를 고친 뒤로는
  아직 끝까지 안 돌려봤다. 타입 검사는 컴파일러만 확정한다.
- **실측 재확인.** `verify/claude-daily.py`를 돌려야 하루 105M · 토큰당 6,957이
  실제 분포와 맞는지 확정된다. 지금 값은 한 달치 합계에서 나온 추정이다.
- **후반 클릭 노동.** 9→10단계는 128,000mL인데 물 한 개가 최근 하루 성장의 1/5라 여러 번 눌러야 한다.
  "모두 쓰기" 경로가 필요하다.
- **본체 포크.** 지금은 독립 패키지. PokeTokenBar에 이식하면 11개 도구 로그 파서와
  릴리스 스크립트를 그대로 쓴다.
- **아트 잔여.** 정원 장식 스프라이트 일부, 종 팔레트 확장.

---

구조는 [chattymin/PokeTokenBar](https://github.com/chattymin/PokeTokenBar)(MIT)를 따르되
포켓몬 레이어를 식물·정원으로 교체했다. 원저작권 © 2026 chattymin.
